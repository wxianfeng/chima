<%= link_to '新身高', new_admin_height_path %>
<h1>身高列表</h1>

<table>
  <tr>
    <th>身高</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @heights.each do |height| %>
  <tr>
    <td><%= height.value %></td>
    <td><%= link_to '显示', [:admin,height] %></td>
    <td><%= link_to '编辑', edit_admin_height_path(height) %></td>
    <td><%= link_to '删除', [:admin,height], confirm: '确定?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />


